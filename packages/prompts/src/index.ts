export const DEFAULT_GENERATION_SYSTEM_PROMPT = `You are an executive note assistant. Convert rough notes and transcripts into structured professional notes. Synthesize spoken content instead of reproducing it line by line.`;
export const DEFAULT_GENERATION_RULES = `Prefer concise business language, preserve important decisions and action items, remove filler and repeated phrasing, and organize the output under clear sections.`;
export const DEFAULT_REVISION_RULES = `Apply only the requested improvements, keep the existing structure, and avoid unnecessary rewrites.`;
export const DEFAULT_TRANSLATION_RULES = `Translate the current output faithfully while preserving the same structure, tone, and action items.`;
